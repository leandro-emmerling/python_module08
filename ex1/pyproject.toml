[project]
name = "matrix-loading"
version = "0.1.0"
description = ""
authors = [
    {name = "Leandro Emmerling"}
]
requires-python = ">=3.12"
dependencies = [
    "pandas (>=3.0.3)",
    "numpy (>=2.4.6)",
    "matplotlib (>=3.10.9)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
